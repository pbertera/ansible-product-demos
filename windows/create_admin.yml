---
- name: Create Admin User
  hosts: "{{ _hosts | default('windows') }}"
  vars:
    iis_message: undef

  tasks:
    - name: Change password
      ansible.windows.win_user:
        name: "{{ user_name }}"
        password: "{{ password }}"
        groups:
         - Administrators
        state: present
